#include <string>

using namespace std;

class Model
{
    public:
        Model();
        ~Model();
        string translateSingleCharacter(char character);
        string translateDoubleCharacter(char character);

    private:

};